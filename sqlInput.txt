-- Test SELECT query
INSERT INTO logs(text) VALUES ('Hello\n\rWorldTest');
INSERT INTO flags (bits) VALUES (B'1101');
INSERT INTO products (color) VALUES (0xABCDEF123);
SELECT name, age, salary
FROM employees
WHERE id = 10
  AND salary >= 5000.75
  AND active = TRUE;

/* Insert test */
INSERT INTO accounts (username, password, balance)
VALUES ('kha\nled', 'mypassword', 1200.50);

/* Update test */
UPDATE accounts
SET balance = balance + 250
-- WHERE username = 'khaled';

/* String test with escape */
SELECT 'hello ''world''' AS greeting;

/* Float, decimal, int */
SELECT 10, 3.1415, 5e2;

/* Operators test */
SELECT a + b * c - d / e, a >= 10, b <= 20, c != 30;

/* Identifier formats test */
SELECT first_name, last_name, _hiddenValue123;

/* Final test */
SELECT * FROM table1;
